<!DOCTYPE html>
<html lang="en" dir="<%= isUrdu ? 'rtl' : 'ltr' %>">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/styles/index.css">
    <title>TareekhDaan - <%= title %></title>
    <% if (title === 'Add Blog' || title === 'Update Blog') { %>
      <link rel="stylesheet" href="https://pro.fontawesome.com/releases/v5.9.0/css/all.css" integrity="sha384-vlOMx0hKjUCl4WzuhIhSNZSm2yQCaf0mOU1hEDK/iztH3gU4v5NMmJln9273A6Jz" crossorigin="anonymous">
    <% } %>
    <% if (isUrdu) { %>
      <style media="screen">
        @font-face {
          font-family: myUrdu;
          src: url("/font/isurdu.ttf");
        }
        .btn, .newsTitle, .newsDesc, .s_h2, .catg_card_title, .title-main, .title-main span, .lang, h1, .sf_card > *, button, input, select, #output {
          font-family: myUrdu !important;
          word-spacing: 2px;
          font-weight: bold;
        }

        .catg_card_title,
        .newsDesc,
        .sf_desc {
          font-size: 1.9rem;
        }

        h1 {
          margin-bottom: 1.5rem;
        } .title-main span {
          font-size: 1.6rem;
          font-weight: bold;
        }
        *:not(:is(footer, footer span)) {
          letter-spacing: 0 !important;
        }
      </style>
    <% } %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script type="text/javascript">
      window.onload = () => {
        if (!!document.querySelector('.newsDesc')) {
          document.querySelectorAll('.newsDesc').forEach(blog => {
            blog.innerText = blog.innerText.substr(0, 50).replaceAll('\n', ' ') +'.....';
          });
        }
      }
    </script>
  </head>
